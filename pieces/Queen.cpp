#include "Queen.h"
#include <cmath>

Queen::Queen(int color, Pos pos) {
    this->type = 4;
    this->color = color;
    this->pos = pos;
}

std::vector<std::string> Queen::legal_moves(Piece* board[8][8]) {
	exit(0);
}
